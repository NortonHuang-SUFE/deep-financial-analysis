# Model profiles and agent bindings live in model-routing.yaml.
# 模型 profile 与 agent 绑定统一配置在 model-routing.yaml。
# model-routing.yaml stores only api_key_env variable names; live keys stay here.
# model-routing.yaml 只保存 api_key_env 变量名；真实模型密码放在这里。
DASHSCOPE_API_KEY=
ARK_API_KEY=
ARK_STANDARD_API_KEY=

# Shared artifact storage root for all agents (LOCAL mode only).
# 所有 agent 共用的产物存储根目录（仅 local 模式生效）。
# Leave empty to write under this repository workspace.
# 留空则写入本仓库工作区。
AGENT_FILE_STORAGE_ROOT=

# Backend mode for the deep-agents filesystem.
# deep-agents 文件系统的后端模式。
#   local   (default) - host/container filesystem
#   local   （默认）  - 本机/容器文件系统
#   daytona           - remote Daytona sandbox
#   daytona           - 远端 Daytona 沙箱
AGENT_BACKEND=local

# Daytona credentials. Only used when AGENT_BACKEND=daytona.
# Daytona 凭证。仅在 AGENT_BACKEND=daytona 时使用。
# DAYTONA_API_KEY=
# DAYTONA_API_URL=https://app.daytona.io/api
# DAYTONA_TARGET=
# DAYTONA_FILE_STORAGE_ROOT=/home/daytona/daily-report

# iFind MCP uses one shared credential by default.
# iFind MCP 默认使用一个共享凭证。
# Use IFIND_MCP_TOKEN for Authorization: Bearer <token>.
# 当需要以 Authorization: Bearer <token> 形式发送时，使用 IFIND_MCP_TOKEN。
IFIND_MCP_TOKEN=
# Use IFIND_MCP_AUTHORIZATION when iFind gives the complete raw Authorization header.
# 当 iFind 给出完整原始 Authorization 头时，使用 IFIND_MCP_AUTHORIZATION。
# IFIND_MCP_AUTHORIZATION=

# Optional per-server URL/transport overrides. Credentials stay shared above.
# 可选的按服务器 URL/传输方式覆盖。凭证仍沿用上面的共享配置。
# IFIND_STOCK_MCP_URL=
# IFIND_STOCK_MCP_TRANSPORT=streamable_http

# Eastmoney 妙想 MX DS MCP credential. Sent as the `em_api_key` header for
# `mx-ds-mcp`; keep the live key only in .env.
# 东方财富妙想 MX DS MCP 凭证。作为 `mx-ds-mcp` 的 `em_api_key` header 发送；
# 真实 key 只放在 .env。
MX_DS_MCP_API_KEY=
# Optional endpoint override for the MX DS MCP server.
# MX DS MCP server 的可选地址覆盖。
# MX_DS_MCP_URL=https://mxapi.eastmoney.com/mxds/mcp
# MX_DS_MCP_TRANSPORT=streamable-http

# HTML renderer browser. Docker sets this to /usr/bin/chromium through
# langgraph.json. On macOS local runs, leave empty to auto-detect Chrome/Edge.
# HTML renderer 浏览器。Docker 会通过 langgraph.json 设置为 /usr/bin/chromium；
# macOS 本地运行可留空，让脚本自动发现 Chrome/Edge。
# HTML_IMAGE_RENDERER_BROWSER=/usr/bin/chromium

# MCP endpoints, agent runtime defaults, tool grants, and external-tool
# concurrency limits live in the tracked tool-concurrency.yaml at the workspace
# root. Keep that file present; only an absent/empty `groups` section disables
# concurrency limiting.
# MCP endpoint、agent 运行默认值、工具授权与外部工具并发上限统一配置在工作区
# 根目录已入库的 tool-concurrency.yaml。请保留该文件；只有缺少或清空
# `groups` 段时，才表示不做并发限制。
# TOOL_CONCURRENCY_CONFIG=

# Local development switches.
# 本地开发开关。
# DAILY_REPORT_TEST_MODE=1
# MORNING_NOTE_TEST_MODE=1
# MORNING_NOTE_DISABLE_MCP=1
# MORNING_NOTE_OUTPUT_TIMESTAMP=20260602-090000
# HTML_IMAGE_RENDERER_TEST_MODE=1

LANGCHAIN_TRACING_V2=true
LANGSMITH_TRACING=true
LANGSMITH_ENDPOINT=https://api.smith.langchain.com
LANGCHAIN_API_KEY=
LANGCHAIN_PROJECT=
