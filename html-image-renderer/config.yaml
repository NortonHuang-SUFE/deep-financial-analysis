# HTML Image Renderer - Configuration
# Safe defaults only. Put API keys in the workspace root ../.env.

model:
  default: "qwen-3.7-max"
  max_tokens: 24000
  base_url: "https://dashscope.aliyuncs.com/compatible-mode"
  api_key: ""
  thinking: "auto"

output:
  dir: "./out"
